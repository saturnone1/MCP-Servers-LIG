apiVersion: v1
kind: Service
metadata:
  name: mcp-kubernetes
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-kubernetes
    app.kubernetes.io/component: mcp-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-kubernetes
  ports:
    - name: http
      port: 8080
      targetPort: http
