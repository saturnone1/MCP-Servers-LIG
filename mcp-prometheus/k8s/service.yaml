apiVersion: v1
kind: Service
metadata:
  name: mcp-prometheus
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-prometheus
    app.kubernetes.io/component: mcp-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-prometheus
    app.kubernetes.io/component: mcp-server
  ports:
    - name: http
      port: 8080
      targetPort: http

