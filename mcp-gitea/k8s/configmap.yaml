apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-gitea-config
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-gitea
    app.kubernetes.io/component: mcp-server
data:
  GITEA_BASE_URL: "https://gitea.example.local"
  MCP_ENABLE_GITEA_WRITES: "true"
