apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-loki-config
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-loki
    app.kubernetes.io/component: mcp-server
data:
  LOKI_BASE_URL: "http://loki-gateway.monitoring.svc.cluster.local"

