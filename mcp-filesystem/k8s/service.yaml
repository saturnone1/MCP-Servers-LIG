apiVersion: v1
kind: Service
metadata:
  name: mcp-filesystem
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-filesystem
    app.kubernetes.io/component: mcp-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-filesystem
  ports:
    - name: http
      port: 8080
      targetPort: http
