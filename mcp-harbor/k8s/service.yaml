apiVersion: v1
kind: Service
metadata:
  name: mcp-harbor
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-harbor
    app.kubernetes.io/component: mcp-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-harbor
    app.kubernetes.io/component: mcp-server
  ports:
    - name: http
      port: 8080
      targetPort: http
