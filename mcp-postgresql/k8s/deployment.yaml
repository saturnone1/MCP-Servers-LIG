apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-postgresql
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-postgresql
    app.kubernetes.io/component: mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mcp-postgresql
      app.kubernetes.io/component: mcp-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mcp-postgresql
        app.kubernetes.io/component: mcp-server
    spec:
      containers:
        - name: mcp-postgresql
          image: local/mcp-postgresql:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: mcp-postgresql-config
            - secretRef:
                name: mcp-postgresql-auth
                optional: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi

