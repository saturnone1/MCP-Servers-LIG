apiVersion: v1
kind: Secret
metadata:
  name: mcp-postgresql-auth
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-postgresql
    app.kubernetes.io/component: mcp-server
type: Opaque
stringData:
  POSTGRES_CONNECTION_STRING: "Host=postgresql.default.svc.cluster.local;Database=postgres;Username=postgres;Password=change-me"

